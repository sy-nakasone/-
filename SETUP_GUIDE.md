# 🚀 セットアップガイド

**所要時間: 約20分**

---

## 📋 このアプリでできること

| 機能 | 説明 |
|------|------|
| 📤 **ファイルアップロード** | Webアプリから音声をアップロード |
| 🏢 **部署選択** | どの部署が利用したか記録 |
| 📁 **フォルダ選択** | Googleドライブのフォルダと連動 |
| 📊 **利用履歴** | 誰がいつ利用したか確認可能 |
| 📈 **部署別統計** | チーム別の利用頻度を可視化 |
| ⚙️ **簡単管理** | スプレッドシートで部署・フォルダを編集 |

---

## Step 1: 管理用スプレッドシートを作成（5分）

### 1-1. Googleスプレッドシートを新規作成

1. [Googleスプレッドシート](https://sheets.google.com) を開く
2. 「空白」で新規作成
3. 名前を「議事録システム管理」などに変更

### 1-2. シートを作成

以下の **3つのシート** を作成してください：

#### シート1: `部署マスタ`

| A列（ID） | B列（部署名） |
|-----------|--------------|
| id | name |
| sales | 営業部 |
| dev | 開発部 |
| hr | 人事部 |
| marketing | マーケティング部 |
| finance | 経理部 |

※1行目はヘッダー、2行目以降にデータを入力

#### シート2: `フォルダマスタ`

| A列（ID） | B列（フォルダ名） | C列（GoogleドライブフォルダID） |
|-----------|------------------|-------------------------------|
| id | name | drive_folder_id |
| regular | 定例会議 | （GoogleドライブのフォルダID） |
| project_a | プロジェクトA | （GoogleドライブのフォルダID） |
| other | その他 | （GoogleドライブのフォルダID） |

※C列のフォルダIDは、Googleドライブでフォルダを開いたときのURLの `folders/` 以降の文字列

#### シート3: `利用履歴`

| A列 | B列 | C列 | D列 | E列 | F列 |
|-----|-----|-----|-----|-----|-----|
| 日時 | 部署 | フォルダ | ファイル名 | ステータス | 議事録URL |

※1行目にヘッダーのみ入力。データは自動で追加されます。

### 1-3. スプレッドシートIDをメモ

URLの `/d/` と `/edit` の間の文字列がIDです：

```
https://docs.google.com/spreadsheets/d/XXXXXXXXXXXXX/edit
                                       ↑この部分がID
```

---

## Step 2: Googleドライブにフォルダを作成（3分）

議事録を保存するフォルダを作成：

1. [Googleドライブ](https://drive.google.com) を開く
2. 必要なフォルダを作成（例: 定例会議、プロジェクトA、その他）
3. 各フォルダのIDをスプレッドシートの「フォルダマスタ」に入力

---

## Step 3: Gemini APIキーを取得（2分）

1. [Google AI Studio](https://aistudio.google.com/apikey) にアクセス
2. 「Create API Key」をクリック
3. 生成されたAPIキーをコピー

---

## Step 4: デプロイ（10分）

### 4-1. Google Cloud SDKをインストール（未インストールの場合）

**Mac:**
```bash
brew install google-cloud-sdk
```

**Windows:**
[インストーラー](https://cloud.google.com/sdk/docs/install) をダウンロード

### 4-2. ログイン

```bash
gcloud auth login
```

### 4-3. デプロイ実行

```bash
cd app
chmod +x deploy.sh
./deploy.sh
```

対話形式で入力:
1. GCPプロジェクトID
2. Gemini APIキー
3. スプレッドシートID
4. Chatwork設定（任意）

---

## Step 5: 権限設定（3分）

デプロイ完了時に表示される **サービスアカウント** を共有：

### スプレッドシートに共有
1. スプレッドシートを開く
2. 右上の「共有」をクリック
3. サービスアカウントのメールを入力
4. 「編集者」を選択

### Googleドライブフォルダに共有
1. 各フォルダを右クリック →「共有」
2. サービスアカウントのメールを入力
3. 「編集者」を選択

---

## ✅ 完了！

デプロイ時に表示されたURLにアクセスして、アプリを使用できます。

---

## 📖 使い方

### ファイルをアップロード

1. アプリにアクセス
2. 部署を選択
3. フォルダを選択
4. 音声ファイルをアップロード
5. 「議事録を生成」をクリック

### 利用履歴を確認

1. 左メニューから「📊 利用履歴・統計」を選択
2. 部署別の利用回数グラフを確認
3. 過去の処理履歴を検索・フィルター

### 部署・フォルダを追加

1. 管理用スプレッドシートを開く
2. 「部署マスタ」または「フォルダマスタ」シートに行を追加
3. アプリを再読み込みで反映

---

## 🔧 トラブルシューティング

### 「部署が表示されない」

→ スプレッドシートがサービスアカウントに共有されているか確認

### 「ファイルがアップロードできない」

→ Googleドライブフォルダがサービスアカウントに共有されているか確認

### 「履歴が記録されない」

→ スプレッドシートの「利用履歴」シートが存在するか確認

### ログを確認

```bash
gcloud logs read --service meeting-minutes-api --limit 50
```
