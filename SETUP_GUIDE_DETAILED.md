# 📖 詳細セットアップガイド（ノンプログラマー向け）

このガイドでは、プログラミング知識がなくても設定できるよう、すべての手順を詳しく説明します。

**所要時間: 約30〜40分**

---

## 📋 目次

1. [Googleスプレッドシートの作成](#step-1-googleスプレッドシートの作成15分)
2. [Googleドライブのフォルダ作成](#step-2-googleドライブのフォルダ作成5分)
3. [Gemini APIキーの取得](#step-3-gemini-apiキーの取得5分)
4. [GCPプロジェクトの作成](#step-4-gcpプロジェクトの作成5分)
5. [Google Cloud SDKのインストール](#step-5-google-cloud-sdkのインストール5分)
6. [アプリのデプロイ](#step-6-アプリのデプロイ10分)
7. [権限設定](#step-7-権限設定5分)
8. [動作確認](#step-8-動作確認)

---

## Step 1: Googleスプレッドシートの作成（15分）

このスプレッドシートで、部署やフォルダを管理します。

### 1-1. 新しいスプレッドシートを作成

1. ブラウザで [https://sheets.google.com](https://sheets.google.com) を開く
2. 左上の「**＋ 空白**」をクリック
3. 新しいスプレッドシートが開く

### 1-2. スプレッドシートの名前を変更

1. 左上の「**無題のスプレッドシート**」をクリック
2. 「**議事録システム管理**」と入力
3. Enterキーを押す

### 1-3. 「部署マスタ」シートを作成

#### シート名を変更
1. 画面下部の「**シート1**」タブを右クリック
2. 「**名前を変更**」をクリック
3. 「**部署マスタ**」と入力してEnter

#### データを入力
以下の内容を入力してください：

**A1セル**に `id` と入力  
**B1セル**に `name` と入力

**A2セル**から順番に：
```
A2: sales       B2: 営業部
A3: dev         B3: 開発部
A4: hr          B4: 人事部
A5: marketing   B5: マーケティング部
A6: finance     B6: 経理部
A7: planning    B7: 企画部
A8: support     B8: カスタマーサポート部
```

※ 会社の部署に合わせて変更・追加してください

#### 見た目を整える（任意）
1. A1:B1を選択（A1をクリック→Shiftを押しながらB1をクリック）
2. 上部メニューの「**太字（B）**」ボタンをクリック
3. 背景色ボタン（ペンキ缶アイコン）をクリック→好きな色を選択

### 1-4. 「フォルダマスタ」シートを作成

#### 新しいシートを追加
1. 画面下部の「**＋**」ボタン（シートタブの右側）をクリック
2. 新しい「シート2」が作成される
3. 「シート2」を右クリック→「**名前を変更**」
4. 「**フォルダマスタ**」と入力してEnter

#### データを入力
**A1セル**に `id` と入力  
**B1セル**に `name` と入力  
**C1セル**に `drive_folder_id` と入力

**A2セル**から順番に：
```
A2: regular      B2: 定例会議        C2: （後で入力）
A3: project_a    B3: プロジェクトA    C3: （後で入力）
A4: project_b    B4: プロジェクトB    C4: （後で入力）
A5: company      B5: 全体会議        C5: （後で入力）
A6: other        B6: その他          C6: （後で入力）
```

※ C列（GoogleドライブフォルダID）は Step 2 で入力します

### 1-5. 「利用履歴」シートを作成

#### 新しいシートを追加
1. 画面下部の「**＋**」ボタンをクリック
2. 「シート3」を右クリック→「**名前を変更**」
3. 「**利用履歴**」と入力してEnter

#### ヘッダーを入力
1行目に以下を入力：
```
A1: 日時
B1: 部署
C1: フォルダ
D1: ファイル名
E1: ステータス
F1: 議事録URL
```

※ このシートには自動でデータが追加されます

### 1-6. スプレッドシートIDをメモ

⚠️ **重要: このIDは後で使います！**

1. ブラウザのアドレスバーを見る
2. URLは以下のような形式：
   ```
   https://docs.google.com/spreadsheets/d/XXXXXXXXXXXXXXXXXX/edit
   ```
3. `/d/` と `/edit` の間の文字列（XXXXXXXXXXXXXXXXXX の部分）をコピー
4. メモ帳などに貼り付けて保存

**例：**
```
URL: https://docs.google.com/spreadsheets/d/1ABC123def456GHI789jkl/edit
                                           ↑ここから↑ここまで
スプレッドシートID: 1ABC123def456GHI789jkl
```

---

## Step 2: Googleドライブのフォルダ作成（5分）

議事録を保存するフォルダを作成します。

### 2-1. Googleドライブを開く

1. ブラウザで [https://drive.google.com](https://drive.google.com) を開く

### 2-2. 「議事録」フォルダを作成

1. 左上の「**＋ 新規**」ボタンをクリック
2. 「**新しいフォルダ**」をクリック
3. 「**議事録**」と入力
4. 「**作成**」をクリック

### 2-3. サブフォルダを作成

1. 作成した「議事録」フォルダをダブルクリックして開く
2. 再度「**＋ 新規**」→「**新しいフォルダ**」
3. 「**定例会議**」と入力→「作成」
4. 同様に以下のフォルダも作成：
   - プロジェクトA
   - プロジェクトB
   - 全体会議
   - その他

### 2-4. フォルダIDを取得してスプレッドシートに入力

各フォルダのIDを取得し、スプレッドシートの「フォルダマスタ」シートに入力します。

#### 「定例会議」フォルダのIDを取得
1. 「定例会議」フォルダをダブルクリックして開く
2. ブラウザのアドレスバーを見る
3. URLは以下のような形式：
   ```
   https://drive.google.com/drive/folders/XXXXXXXXXXXXXXXXXX
   ```
4. `folders/` の後の文字列をコピー

#### スプレッドシートに入力
1. 先ほど作成したスプレッドシートを開く
2. 「フォルダマスタ」シートをクリック
3. 「定例会議」の行のC列（C2セル）にフォルダIDを貼り付け

#### 他のフォルダも同様に
- プロジェクトA → C3セルに入力
- プロジェクトB → C4セルに入力
- 全体会議 → C5セルに入力
- その他 → C6セルに入力

---

## Step 3: Gemini APIキーの取得（5分）

AIの機能を使うためのキーを取得します。

### 3-1. Google AI Studioにアクセス

1. ブラウザで [https://aistudio.google.com/apikey](https://aistudio.google.com/apikey) を開く
2. Googleアカウントでログイン（まだの場合）

### 3-2. APIキーを作成

1. 「**Create API Key**」ボタンをクリック
2. 「Create API key in new project」を選択（または既存プロジェクトを選択）
3. APIキーが表示される

### 3-3. APIキーをコピー

1. 表示されたAPIキー（`AIza...` で始まる長い文字列）の右側のコピーボタンをクリック
2. メモ帳などに貼り付けて保存

⚠️ **重要: このキーは他人に教えないでください！**

---

## Step 4: GCPプロジェクトの作成（5分）

アプリを動かすためのプロジェクトを作成します。

### 4-1. Google Cloud Consoleにアクセス

1. ブラウザで [https://console.cloud.google.com](https://console.cloud.google.com) を開く
2. Googleアカウントでログイン

### 4-2. 新しいプロジェクトを作成

1. 画面上部の「**プロジェクトを選択**」をクリック
2. 右上の「**新しいプロジェクト**」をクリック
3. プロジェクト名に「**meeting-minutes**」と入力
4. 「**作成**」をクリック
5. 作成完了まで数秒待つ

### 4-3. プロジェクトIDをメモ

1. 作成したプロジェクトを選択
2. 画面左上のプロジェクト名の下に表示されるIDをメモ
   - 例: `meeting-minutes-123456`

### 4-4. 請求先アカウントを設定（初回のみ）

※ 無料枠内で使用できますが、請求先の設定は必要です

1. 左メニューの「**お支払い**」をクリック
2. 「**請求先アカウントをリンク**」をクリック
3. 画面の指示に従ってクレジットカードを登録
   - ※ 無料枠内なら課金されません

---

## Step 5: Google Cloud SDKのインストール（5分）

コマンドでGoogleのサービスを操作するためのツールをインストールします。

### Windowsの場合

#### 5-1. インストーラーをダウンロード

1. ブラウザで [https://cloud.google.com/sdk/docs/install](https://cloud.google.com/sdk/docs/install) を開く
2. 「**Windows**」タブをクリック
3. 「**Google Cloud CLI インストーラ**」をクリックしてダウンロード

#### 5-2. インストール

1. ダウンロードしたファイル（`GoogleCloudSDKInstaller.exe`）をダブルクリック
2. 「Next」を何度かクリックしてインストール
3. 最後に「Run 'gcloud init'」にチェックを入れて「Finish」

#### 5-3. 初期設定

1. コマンドプロンプトが開く
2. ブラウザが開いてGoogleログイン画面が表示される
3. Googleアカウントでログイン
4. 「許可」をクリック
5. コマンドプロンプトに戻る
6. プロジェクトを選択する画面で、先ほど作成したプロジェクトの番号を入力

### Macの場合

#### 5-1. ターミナルを開く

1. Finderを開く
2. 「アプリケーション」→「ユーティリティ」→「ターミナル」をダブルクリック

#### 5-2. Homebrewでインストール

以下のコマンドを1行ずつコピーして、ターミナルに貼り付けてEnter：

```bash
brew install google-cloud-sdk
```

※ Homebrewがない場合は先にインストール：
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

#### 5-3. 初期設定

```bash
gcloud init
```

1. ブラウザが開いてGoogleログイン画面が表示される
2. Googleアカウントでログイン
3. 「許可」をクリック
4. ターミナルに戻り、プロジェクトを選択

---

## Step 6: アプリのデプロイ（10分）

⚠️ **この手順を詳しく説明します。落ち着いて1つずつ進めてください。**

### 6-1. アプリファイルをダウンロード

#### 6-1-1. GitHubからダウンロード

1. ブラウザで以下のURLを開く：
   ```
   https://github.com/sy-nakasone/-/tree/cursor/ai-meeting-minutes-summarization-bef7
   ```

2. 緑色の「**Code**」ボタンをクリック

3. 「**Download ZIP**」をクリック

4. ZIPファイルがダウンロードされる

#### 6-1-2. ZIPファイルを展開

**Windowsの場合：**
1. ダウンロードフォルダを開く
2. ダウンロードしたZIPファイルを右クリック
3. 「**すべて展開**」をクリック
4. 展開先を「**デスクトップ**」に変更
5. 「**展開**」をクリック

**Macの場合：**
1. ダウンロードフォルダを開く
2. ダウンロードしたZIPファイルをダブルクリック
3. 自動的に展開される
4. 展開されたフォルダをデスクトップに移動

#### 6-1-3. フォルダ名を確認

展開すると以下のようなフォルダができます：
```
デスクトップ/
  └── --cursor-ai-meeting-minutes-summarization-bef7/
       └── app/
            ├── api.py
            ├── deploy.sh
            ├── Dockerfile
            ├── Dockerfile.streamlit
            ├── requirements.txt
            └── streamlit_app.py
```

---

### 6-2. コマンドプロンプト/ターミナルを開く

#### Windowsの場合

**方法1: スタートメニューから**
1. キーボードの「Windowsキー」を押す
2. 「**cmd**」と入力
3. 「**コマンドプロンプト**」をクリック

**方法2: 検索から**
1. タスクバーの検索ボックスに「**コマンドプロンプト**」と入力
2. 表示された「コマンドプロンプト」をクリック

#### Macの場合

**方法1: Spotlightから**
1. 「Command + Space」を押す
2. 「**ターミナル**」と入力
3. 「ターミナル」をクリック

**方法2: Finderから**
1. Finderを開く
2. 「アプリケーション」→「ユーティリティ」→「ターミナル」をダブルクリック

---

### 6-3. appフォルダに移動

黒い画面（コマンドプロンプト/ターミナル）が開いたら、appフォルダに移動します。

#### Windowsの場合

以下のコマンドをコピーして貼り付け、Enterを押します：

```
cd Desktop\--cursor-ai-meeting-minutes-summarization-bef7\app
```

⚠️ **エラーが出る場合：**
フォルダ名が違う可能性があります。以下を試してください：

1. デスクトップにあるフォルダ名を確認
2. フォルダ名に合わせてコマンドを修正

```
cd Desktop\（実際のフォルダ名）\app
```

#### Macの場合

以下のコマンドをコピーして貼り付け、Enterを押します：

```
cd ~/Desktop/--cursor-ai-meeting-minutes-summarization-bef7/app
```

⚠️ **エラーが出る場合：**
```
cd ~/Desktop/
ls
```
と入力して、実際のフォルダ名を確認してください。

---

### 6-4. 正しいフォルダにいるか確認

以下のコマンドを入力してEnter：

**Windowsの場合：**
```
dir
```

**Macの場合：**
```
ls
```

以下のファイルが表示されればOKです：
```
api.py
deploy.sh
Dockerfile
Dockerfile.streamlit
requirements.txt
streamlit_app.py
```

---

### 6-5. デプロイスクリプトを実行

#### Windowsの場合

以下のコマンドを入力してEnter：

```
bash deploy.sh
```

⚠️ **「bash が認識されていません」とエラーが出る場合：**

Git Bashを使用します：

1. スタートメニューで「**Git Bash**」を検索
2. Git Bashを開く
3. 以下のコマンドを入力：
   ```
   cd /c/Users/（ユーザー名）/Desktop/--cursor-ai-meeting-minutes-summarization-bef7/app
   chmod +x deploy.sh
   ./deploy.sh
   ```

※ Git Bashがない場合は、[https://git-scm.com/download/win](https://git-scm.com/download/win) からインストール

#### Macの場合

以下のコマンドを順番に入力してEnter：

```
chmod +x deploy.sh
./deploy.sh
```

---

### 6-6. 対話形式で情報を入力

スクリプトが起動すると、順番に質問されます。各質問に対して、メモしておいた情報を入力します。

#### 質問1: GCPプロジェクトID

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[1/5] GCPプロジェクトID
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
プロジェクトID: 
```

**入力するもの:** Step 4でメモした「プロジェクトID」
**例:** `meeting-minutes-123456`

入力したらEnterを押す

---

#### 質問2: Gemini APIキー

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[2/5] Gemini APIキー
取得先: https://aistudio.google.com/apikey
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
APIキー: 
```

**入力するもの:** Step 3でメモした「Gemini APIキー」
**例:** `AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX`

入力したらEnterを押す

---

#### 質問3: スプレッドシートID

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[3/5] 管理用スプレッドシートID
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
スプレッドシートID: 
```

**入力するもの:** Step 1でメモした「スプレッドシートID」
**例:** `1ABC123def456GHI789jkl`

入力したらEnterを押す

---

#### 質問4: Chatwork設定（任意）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[4/5] Chatwork設定（任意・Enterでスキップ）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Chatwork APIトークン: 
Chatwork ルームID: 
```

**Chatwork通知が必要な場合:** トークンとルームIDを入力
**不要な場合:** 何も入力せずにEnterを2回押す

---

#### 質問5: 確認

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
設定内容の確認
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  プロジェクトID: meeting-minutes-123456
  リージョン: asia-northeast1
  スプレッドシートID: 1ABC123def456GHI789jkl
  Chatwork: なし

この設定でデプロイしますか？ (y/n): 
```

**入力するもの:** `y` と入力してEnter

---

### 6-7. デプロイの進行を待つ

以下のようなメッセージが順番に表示されます。**5〜10分ほど待ちます。**

```
[5/5] デプロイ中...
APIを有効化中...
APIサーバーをデプロイ中...
```

途中で以下のような質問が出る場合があります：

```
Do you want to continue (Y/n)?
```

**入力するもの:** `Y` と入力してEnter

---

### 6-8. デプロイ完了の確認

デプロイが成功すると、以下のような画面が表示されます：

```
╔════════════════════════════════════════════════════════╗
║   ✅ デプロイ完了！                                     ║
╚════════════════════════════════════════════════════════╝

🌐 アプリURL:
   https://meeting-minutes-app-xxxxx-an.a.run.app

🔧 API URL:
   https://meeting-minutes-api-xxxxx-an.a.run.app

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️  重要: 以下の設定を行ってください
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

サービスアカウント:
   123456789-compute@developer.gserviceaccount.com
```

⚠️ **以下の2つをメモしてください：**

1. **アプリURL** - アプリにアクセスするためのURL
2. **サービスアカウント** - 次のステップで使用するメールアドレス

---

### 6-9. よくあるエラーと対処法

#### エラー: 「gcloud: command not found」

**原因:** Google Cloud SDKがインストールされていない、またはパスが通っていない

**対処:**
1. Step 5に戻ってSDKをインストール
2. コマンドプロンプト/ターミナルを閉じて、もう一度開く

---

#### エラー: 「ERROR: (gcloud.run.deploy) PERMISSION_DENIED」

**原因:** GCPの請求先アカウントが設定されていない

**対処:**
1. [https://console.cloud.google.com/billing](https://console.cloud.google.com/billing) を開く
2. プロジェクトに請求先アカウントをリンク

---

#### エラー: 「bash: deploy.sh: No such file or directory」

**原因:** 現在のフォルダが間違っている

**対処:**
1. `pwd`（Mac）または `cd`（Windows）で現在地を確認
2. 6-3の手順でappフォルダに移動し直す

---

#### エラー: 「The project does not exist」

**原因:** プロジェクトIDが間違っている

**対処:**
1. [https://console.cloud.google.com](https://console.cloud.google.com) でプロジェクトIDを再確認
2. もう一度 `./deploy.sh` を実行

---

## Step 7: 権限設定（5分）

アプリがスプレッドシートとGoogleドライブにアクセスできるようにします。

### 7-1. スプレッドシートにサービスアカウントを追加

1. Step 1で作成したスプレッドシートを開く
2. 右上の「**共有**」ボタン（緑色）をクリック
3. 「ユーザーやグループを追加」欄に、Step 6でメモした**サービスアカウントのメールアドレス**を貼り付け
4. 右側のプルダウンで「**編集者**」を選択
5. 「**送信**」をクリック

### 7-2. Googleドライブのフォルダにサービスアカウントを追加

Step 2で作成した「議事録」フォルダと、そのサブフォルダすべてに共有設定を行います。

#### 親フォルダ（議事録）を共有
1. Googleドライブを開く
2. 「議事録」フォルダを右クリック
3. 「**共有**」をクリック
4. サービスアカウントのメールアドレスを入力
5. 「**編集者**」を選択
6. 「**送信**」をクリック

#### サブフォルダにも適用されているか確認
通常、親フォルダを共有するとサブフォルダにも権限が継承されますが、念のため確認してください。

---

## Step 8: 動作確認

### 8-1. アプリにアクセス

1. ブラウザで Step 6 でメモした **アプリURL** を開く
   ```
   https://meeting-minutes-app-xxxxx-an.a.run.app
   ```

2. アプリ画面が表示されれば成功！

### 8-2. 部署とフォルダが表示されるか確認

1. 「🏢 部署を選択」のプルダウンをクリック
2. スプレッドシートに入力した部署が表示されるか確認

3. 「📁 保存先フォルダを選択」のプルダウンをクリック
4. スプレッドシートに入力したフォルダが表示されるか確認

### 8-3. テストアップロード

1. 短い音声ファイル（MP3など）を用意
2. 部署とフォルダを選択
3. ファイルをアップロード
4. 「🚀 議事録を生成」をクリック
5. 処理完了を待つ（数分かかります）
6. 「✅ 議事録の生成が完了しました！」と表示されれば成功！

### 8-4. 議事録を確認

1. Googleドライブを開く
2. 選択したフォルダ内に議事録（Google Docs）が作成されているか確認

### 8-5. 利用履歴を確認

1. アプリで左メニューの「📊 利用履歴・統計」をクリック
2. 先ほどのアップロードが履歴に表示されるか確認

---

## 🎉 セットアップ完了！

おめでとうございます！これでセットアップは完了です。

### 日常の使い方

1. アプリにアクセス
2. 部署を選択
3. 保存先フォルダを選択
4. 音声ファイルをアップロード
5. 「議事録を生成」をクリック
6. 完了を待つ

### 部署やフォルダを追加したい場合

1. 管理用スプレッドシートを開く
2. 「部署マスタ」または「フォルダマスタ」シートに行を追加
3. アプリを再読み込み（F5キー）

---

## ❓ よくあるトラブル

### 「部署が表示されない」

**原因:** スプレッドシートの共有設定が正しくない

**対処:**
1. スプレッドシートを開く
2. 右上の「共有」をクリック
3. サービスアカウントが「編集者」として追加されているか確認
4. されていなければ追加する

### 「ファイルがアップロードできない」

**原因:** Googleドライブフォルダの共有設定が正しくない

**対処:**
1. Googleドライブを開く
2. 該当フォルダを右クリック→「共有」
3. サービスアカウントが追加されているか確認

### 「処理中にエラーが発生」

**原因:** 様々な可能性あり

**対処:**
1. 別の音声ファイルで試してみる
2. ファイルサイズが大きすぎないか確認（まずは小さいファイルで）
3. 音声形式が対応しているか確認（MP3, MP4, WAV, M4A, WEBM, OGG, FLAC）

### 「アプリにアクセスできない」

**原因:** デプロイが完了していないか、URLが間違っている

**対処:**
1. URLを再確認
2. デプロイが完了しているか確認
3. 数分待ってから再度アクセス

---

## 📞 困ったときは

上記で解決しない場合は、以下の情報を添えてシステム管理者に連絡してください：

1. どの手順で問題が発生したか
2. エラーメッセージ（表示されている場合）
3. 使用しているブラウザとOS
